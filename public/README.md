# Public Assets

## Profile Picture

Place your profile picture here as `profile.jpg` (or update the path in `components/ProfilePicture.tsx`).

**Supported formats:** JPG, PNG, WebP

**Recommended size:** 400x400px or larger (square aspect ratio)

**File location:** `public/profile.jpg`

The image will be automatically optimized by Next.js Image component.

